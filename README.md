# Bloc-Notes

A modern, offline-first markdown note-taking app built with React 19, Vite, and IndexedDB. Write in Markdown, preview in real time, and keep your notes stored in the browser—no backend required.

---

## Features

- **Sidebar** — List all notes with title and a short preview (~15 words). One-click new note creation.
- **Live Markdown preview** — Rendered HTML above the editor; supports GFM (tables, strikethrough, etc.) with safe sanitization.
- **Rich editor** — Title and Markdown content with auto-save (debounced) so changes persist without a manual “Save” button.
- **IndexedDB persistence** — Notes are stored in the browser; data survives reloads and works offline. No server or GDPR-sensitive storage.
- **Seed notes** — Optional starter notes generated from the `Resources/` docs; existing user notes are never overwritten.

---

## Tech Stack

| Layer        | Choice                |
| ------------ | --------------------- |
| Build        | Vite 7                 |
| UI           | React 19, JavaScript   |
| Styling      | Tailwind CSS 4        |
| Components   | CVA, clsx, tailwind-merge, Lucide icons |
| State        | Zustand                |
| Persistence  | IndexedDB (idb-keyval) |
| Markdown     | react-markdown, remark-gfm, rehype-raw, rehype-sanitize |
| Forms/validation | React Hook Form, Zod, @hookform/resolvers |
| Tooling      | Biome (lint & format)  |

---

## Project structure

```
bloc-notes/
├── README.md
├── Resources/          # Project specs and guides (Markdown)
│   ├── Projet block notes.md
│   ├── Projet Bonus Formulaire.md
│   ├── recommandation.md
│   ├── Shadcn-UI.md
│   ├── Composants-fonctionnels.md
│   └── Old/            # Archived legacy docs
├── scripts/
│   ├── README.md
│   └── seed-from-resources.js   # Generates seed notes from Resources/
└── bloc-notes-app/    # The React application
    ├── src/
    │   ├── components/   # NoteSidebar, NoteItem, NoteEditor, NotePreview
    │   ├── lib/          # db (IndexedDB), utils
    │   ├── stores/       # Zustand notes store
    │   └── data/         # seedNotes.js (generated by scripts)
    ├── index.html
    ├── vite.config.js
    └── package.json
```

---

## Getting started

### Prerequisites

- Node.js 18+
- npm (or compatible package manager)

### Install and run

```bash
git clone <repository-url>
cd bloc-notes
cd bloc-notes-app
npm install
npm run dev
```

Then open the URL shown in the terminal (e.g. `http://localhost:5173`).

### Build for production

```bash
cd bloc-notes-app
npm run build
npm run preview   # optional: local preview of the build
```

---

## Scripts (bloc-notes-app)

| Command        | Description                                      |
| -------------- | ------------------------------------------------- |
| `npm run dev`  | Start dev server (Vite). Seed script runs before. |
| `npm run build`| Production build. Seed script runs before.        |
| `npm run seed` | Regenerate `src/data/seedNotes.js` from Resources. |
| `npm run preview` | Serve the production build locally.           |
| `npm run lint` | Run ESLint.                                      |

Seed notes are built from Markdown files in `Resources/` (see `scripts/README.md`). On first load, the app injects any missing seed notes into IndexedDB; existing user notes are left unchanged.

---

## Resources

- **Projet block notes.md** — Original project specification (French).
- **Projet Bonus Formulaire.md** — Bonus form project spec.
- **recommandation.md** — Step-by-step “pro” setup: stack, config, and implementation notes.
- **Shadcn-UI.md**, **Composants-fonctionnels.md** — UI and React patterns used in the app.

---

## License

Private / educational use. See repository or author for terms.
